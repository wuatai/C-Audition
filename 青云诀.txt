GateApp：
	初始化GateApp->开启RMI（远程方法调用）服务
初始化GateApp:
	加载公共配置->初始化频道->加载游戏配置数据->加载场景->加载任务->初始化接口(RMI)->初始化消息处理(MQ)
	->初始化频道(与各个APP建立session连接)->启动一些定时器(如邮件队列定时器)
	
InterApp:
	初始化InterApp->开启RMI（远程方法调用）服务
初始化InterApp:加载公共配置->初始化频道->加载游戏配置数据->加载场景->初始化接口(RMI)->初始化消息处理(MQ)
	->初始化频道(与各个APP建立session连接)
	
